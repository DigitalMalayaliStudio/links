# Jekyll
remote_theme: linkhub-jekyll-theme

# Site Configuration
name: Digital Malayali Studio  # Name of your website
tagline: Link-in-bio  # Set your preferred page title
description: Explore all the links featured in our Instagram posts here.  # Also used as a meta description
favicon:
  path: assets/images/favicon.png # Path / URL to the favicon of your website (e.g., 'assets/images/favicon.png')
  type: png # File format of image. Change according to your image. Supported types: https://developer.mozilla.org/en-US/docs/Web/Media/Formats/Image_types
logo: assets/images/dp.jpg # Path / URL to the logo (e.g., 'assets/images/logo.png')
url: https://studio.digitalmalayali.in/links # URL of your website
locale: en_GB # The value specifies the locale in which these tags are marked up. It follows the format `language_TERRITORY`.
ga4: # Google Analytics 4 measurement ID, for e.g. G-XXXXXXX (optional). Remove it if it’s not needed.
twitter:
  username: DigiMalayali # Used for SEO
image:
  path: assets/images/dp.jpg # Used for SEO (og:image, twitter:image)
  type: jpg # File format of image.
  height: 100 # Pixels
  width: 100
  alt: logo

# Verified Badge Configuration
badge:
  enable: true # Set to 'true' or 'false' to enable / disable the badge
  icon: ri:verified-badge-fill  # Enter the icon name for the badge from Iconify Design (https://icon-sets.iconify.design/)
  color: '#B682D9'  # Define the color of the badge
 
# Defaults
defaults:
  -
    scope:
      path: ""
    values:
      layout: "default"    

# Exclude
exclude: [README.md, Gemfile.lock, .jekyll-cache/, .github/, CHANGELOG.md, Gemfile, LICENSE.txt, funding.yml, linkhub-jekyll-theme.gemspec]
